# Reference list — the stream-consumer image and the Airflow containers
# install these independently (see stream/Dockerfile and the
# _PIP_ADDITIONAL_REQUIREMENTS env var in docker-compose.yml).
boto3==1.43
